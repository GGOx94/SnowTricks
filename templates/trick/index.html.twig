{% extends 'base.html.twig' %}

{% block title %}Hello TrickController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/xampp/htdocs/p6-snowtricks/src/Controller/TrickController.php'|file_link(0) }}">src/Controller/TrickController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/xampp/htdocs/p6-snowtricks/templates/trick/index.html.twig'|file_link(0) }}">templates/trick/index.html.twig</a></code></li>
    </ul>
</div>

    <h2>{{ trick.title|raw }}</h2>
    <div>
        <p>
            {{ trick.description|raw }}
        </p>
        <small>Trick créé le : {{ trick.createdAt|date("m/d/Y") }}</small>
        <ul>
        {%  for com in trick.comments %}
            <li>
                <h3>{{ com.author.name }}</h3>
                <p>{{ com.content }}</p>
                <small>Publié le : {{ com.createdAt|date("m/d/Y") }}</small>
            </li>

        {% endfor %}
        </ul>
    </div>

{% endblock %}
